<%= form_for current_user.user_friendships.build(friend_id: @user) do |f| %>

  <%= f.hidden_field :friend_id, value: @user.id  %>      
  <%= submit_tag "Study partner request sent", class: "button radius tiny" %>
  
<% end %>